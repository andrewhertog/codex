diff --git a/build/gulpfile.vscode.win32.js b/build/gulpfile.vscode.win32.js
index 98175f5..b27bce1 100644
--- a/build/gulpfile.vscode.win32.js
+++ b/build/gulpfile.vscode.win32.js
@@ -64,13 +64,13 @@ function packageInnoSetup(iss, options, cb) {
  * @param {string} target
  */
 function buildWin32Setup(arch, target) {
-	if (target !== 'system' && target !== 'user') {
+	if (target !== 'system' && target !== 'user' && target !== 'unified') {
 		throw new Error('Invalid setup target');
 	}
 
 	return cb => {
-		const x64AppId = target === 'system' ? product.win32x64AppId : product.win32x64UserAppId;
-		const arm64AppId = target === 'system' ? product.win32arm64AppId : product.win32arm64UserAppId;
+		const x64AppId = target === 'unified' ? product.win32x64AppId : (target === 'system' ? product.win32x64AppId : product.win32x64UserAppId);
+		const arm64AppId = target === 'unified' ? product.win32arm64AppId : (target === 'system' ? product.win32arm64AppId : product.win32arm64UserAppId);
 
 		const sourcePath = buildPath(arch);
 		const outputPath = setupDir(arch, target);
@@ -130,10 +130,9 @@ function defineWin32SetupTasks(arch, target) {
 	gulp.task(task.define(`vscode-win32-${arch}-${target}-setup`, task.series(cleanTask, buildWin32Setup(arch, target))));
 }
 
-defineWin32SetupTasks('x64', 'system');
-defineWin32SetupTasks('arm64', 'system');
-defineWin32SetupTasks('x64', 'user');
-defineWin32SetupTasks('arm64', 'user');
+// Define unified setup tasks that create a single installer supporting both system and user installations
+defineWin32SetupTasks('x64', 'unified');
+defineWin32SetupTasks('arm64', 'unified');
 
 /**
  * @param {string} arch
@@ -157,3 +156,6 @@ function updateIcon(executablePath) {
 
 gulp.task(task.define('vscode-win32-x64-inno-updater', task.series(copyInnoUpdater('x64'), updateIcon(path.join(buildPath('x64'), 'tools', 'inno_updater.exe')))));
 gulp.task(task.define('vscode-win32-arm64-inno-updater', task.series(copyInnoUpdater('arm64'), updateIcon(path.join(buildPath('arm64'), 'tools', 'inno_updater.exe')))));
+
+// The unified setup tasks are already defined above via defineWin32SetupTasks()
+// They can be accessed directly as 'vscode-win32-x64-unified-setup' and 'vscode-win32-arm64-unified-setup'
diff --git a/build/win32/code.iss b/build/win32/code.iss
index cad6e39..6eea467 100644
--- a/build/win32/code.iss
+++ b/build/win32/code.iss
@@ -44,7 +44,11 @@ CloseApplications=force
 SignTool=esrp
 #endif
 
-#if "user" == InstallTarget
+#if "unified" == InstallTarget
+DefaultDirName={code:GetDefaultDirName}
+PrivilegesRequired=lowest
+PrivilegesRequiredOverridesAllowed=dialog
+#elif "user" == InstallTarget
 DefaultDirName={userpf}\{#DirName}
 PrivilegesRequired=lowest
 #else
@@ -1296,6 +1300,14 @@ Root: {#SoftwareClassesRootKey}; Subkey: "Software\Classes\Drive\shell\{#RegValu
 Root: {#EnvironmentRootKey}; Subkey: "{#EnvironmentKey}"; ValueType: expandsz; ValueName: "Path"; ValueData: "{code:AddToPath|{app}\bin}"; Tasks: addtopath; Check: NeedsAddToPath(ExpandConstant('{app}\bin'))
 
 [Code]
+function GetDefaultDirName(Param: String): String;
+begin
+  if IsAdminInstallMode() then
+    Result := ExpandConstant('{pf}\{#DirName}')
+  else
+    Result := ExpandConstant('{userpf}\{#DirName}');
+end;
+
 function IsBackgroundUpdate(): Boolean;
 begin
   Result := ExpandConstant('{param:update|false}') <> 'false';
